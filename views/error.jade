extends layout

block title
  title= (error || 'Error!') + ' | RCS Photography'
  
block content 
  div(class='container')
    div(class='page-header')
      h1 #{ error || 'Error!' }
        br
        small #{ message || 'An error as occurred.' }

