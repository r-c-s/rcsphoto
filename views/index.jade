extends layout

block title
  title= 'RCS Photography'

block content   
  link(rel='stylesheet', href='/css/album.css')

  div(class='container-fluid')
    div(class='page-header')
      h1 Albums
      
  div(class='container-fluid')
    - for (var i = 0; i < albums.length; i++) { 
      a(href='/'+albums[i]._id) #{ albums[i].albumInfo.name }
      br
    - }

 